import * as React from 'react';

const UserDetailsPage = () => {
    return (
        <div data-testid={'user-details-page-testid'}>
            USER DETAILS PAGE
        </div>
    );
};

export default UserDetailsPage;
